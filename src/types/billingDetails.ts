export interface Billing {
  companyName: string;
  buildingName?: string;
  unitNumber?: string;
  streetName: string;
  city: string;
  country: string;
  postalCode: string;
  contactNumber: string;
  companyRegistration?: string;
}
